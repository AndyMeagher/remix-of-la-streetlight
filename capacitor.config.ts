import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lastreetlight.lastreetlight",
  appName: "la-street-light",
  webDir: "dist",
};

export default config;
