import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.andy.lastreetlight",
  appName: "remix-la-street-light",
  webDir: "dist",
};

export default config;
